FROM node:18-alpine

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 모든 의존성 설치 (개발 의존성 포함)
RUN npm install

# 애플리케이션 코드 복사
COPY . .

# 로그 디렉토리 생성
RUN mkdir -p logs uploads

# 포트 노출
EXPOSE 3000

# 개발 모드로 실행 (nodemon 사용)
CMD ["npm", "run", "dev"]

